<resources>
    <!-- Base application theme. -->
    <style name="Theme.UBInOne" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/ub_brown</item>
        <item name="colorPrimaryVariant">@color/black</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <item name="actionMenuTextColor">@color/inverted</item>
        <item name="android:actionMenuTextColor">@color/inverted</item>
    </style>

    <color name="nav">@color/ub_blue</color>
    <color name="inverted">@color/black</color>

    <color name="free">#00B600</color>
    <color name="TBD">#FF8222</color>
    <color name="occupied">#DC0000</color>

    <style name="Theme.UBInOne.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.UBInOne.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.UBInOne.PopupOverlay" parent="ThemeOverlay.AppCompat.DayNight" />
</resources>